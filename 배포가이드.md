# 영수증 자동화 - Cloudflare Workers 배포 가이드

## 1. 사전 준비

### 1.1 Cloudflare 계정 생성
1. https://dash.cloudflare.com/ 접속
2. 계정 생성 (무료)

### 1.2 Node.js 설치
- https://nodejs.org/ 에서 LTS 버전 설치

### 1.3 Wrangler CLI 설치
```bash
npm install -g wrangler
```

### 1.4 Cloudflare 로그인
```bash
wrangler login
```
브라우저가 열리면 로그인하여 인증

---

## 2. R2 버킷 생성

### 2.1 Cloudflare 대시보드에서 생성
1. https://dash.cloudflare.com/ → R2 메뉴
2. "Create bucket" 클릭
3. 버킷 이름: `receipts-storage` (wrangler.toml과 동일해야 함)
4. 생성 완료

### 2.2 또는 CLI로 생성
```bash
wrangler r2 bucket create receipts-storage
```

---

## 3. 프로젝트 설정

### 3.1 의존성 설치
```bash
cd c:\Users\joosu\Desktop\receipt-automation
npm install
```

### 3.2 wrangler.toml 확인
```toml
name = "receipt-automation"
main = "src/index.js"
compatibility_date = "2024-01-01"

[[r2_buckets]]
binding = "RECEIPTS_BUCKET"
bucket_name = "receipts-storage"

[assets]
directory = "./public"
```

---

## 4. 로컬 개발 테스트

```bash
npm run dev
```
또는
```bash
wrangler dev
```

브라우저에서 http://localhost:8787 접속하여 테스트

---

## 5. 배포

### 5.1 Cloudflare에 배포
```bash
npm run deploy
```
또는
```bash
wrangler deploy
```

### 5.2 배포 완료 후
- 제공된 URL로 접속 (예: https://receipt-automation.{계정}.workers.dev)

---

## 6. GitHub 연동 (선택사항)

### 6.1 Git 초기화
```bash
git init
git add .
git commit -m "Initial commit - Cloudflare Workers migration"
```

### 6.2 GitHub 리포지토리 연결
```bash
git remote add origin https://github.com/jooseungun/automation.git
git branch -M main
git push -u origin main
```

### 6.3 Cloudflare Pages 연동 (자동 배포)
1. Cloudflare 대시보드 → Workers & Pages
2. "Connect to Git" 선택
3. GitHub 리포지토리 선택
4. 빌드 설정:
   - Build command: (비워두기)
   - Build output directory: `public`
5. 연결 완료 시 push할 때마다 자동 배포

---

## 7. 프로젝트 구조

```
receipt-automation/
├── src/
│   └── index.js         # Cloudflare Worker 백엔드
├── public/
│   ├── index.html       # 프론트엔드 HTML
│   ├── style.css        # 스타일
│   └── app.js           # 프론트엔드 JS
├── wrangler.toml        # Cloudflare 설정
├── package.json         # Node.js 설정
├── .gitignore           # Git 제외 파일
└── 배포가이드.md        # 이 문서
```

---

## 8. 사용 방법

### 웹 사용법
1. 배포된 URL 접속
2. **프로젝트 생성**: 이름과 설명 입력 후 "프로젝트 생성" 클릭
3. 프로젝트 선택하여 들어가기
4. 영수증 이미지를 드래그 앤 드롭으로 업로드
5. "업로드된 파일 불러오기" 클릭
6. 각 영수증 정보 입력/수정
7. "한 번에 저장" 클릭 → R2의 프로젝트별 output 폴더에 저장

### 프로젝트 구조 (R2)
```
receipts-storage/
├── projects.json                    # 프로젝트 목록
└── projects/
    ├── proj_xxxxx/                  # 프로젝트 1
    │   ├── uploads/                 # 업로드된 원본 파일
    │   ├── output/                  # 처리된 파일
    │   └── analysis_results.json    # 분석 결과
    └── proj_yyyyy/                  # 프로젝트 2
        ├── uploads/
        ├── output/
        └── analysis_results.json
```

### 프로젝트 관리
- 여러 사람이 각자의 프로젝트로 작업 가능
- 프로젝트별로 파일이 완전히 분리됨
- 프로젝트 삭제 시 해당 프로젝트의 모든 파일도 삭제됨

---

## 9. 비용 안내

### Cloudflare Workers (무료 플랜)
- 일 100,000 요청
- 월 10ms CPU 시간

### Cloudflare R2 (무료 포함)
- 10GB 저장 무료
- 월 1,000,000 Class A 작업 무료
- 월 10,000,000 Class B 작업 무료

일반적인 개인 사용에는 무료 플랜으로 충분합니다.

---

## 10. 트러블슈팅

### 오류: R2 버킷을 찾을 수 없음
- wrangler.toml의 `bucket_name`과 실제 생성한 버킷 이름이 일치하는지 확인

### 오류: 인증 실패
```bash
wrangler login
```
재실행

### 로컬 테스트에서 R2 연결 안 됨
- 로컬 개발 시 `wrangler dev`는 임시 R2 시뮬레이션 사용
- 실제 R2 연결: `wrangler dev --remote`

---

## 11. 기존 Flask 버전 실행 (로컬 전용)

기존 Flask 버전은 로컬에서만 사용 가능:
```bash
python app.py
```
브라우저에서 http://localhost:5555 접속
